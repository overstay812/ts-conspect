enum Membership {
  Simple,
  Standart,
  Premium,
}

const membership = Membership.Standart; // 0
const membershipIndexTwo = Membership[2]; // standart

enum SocialMedia {
  VK = "VK",
  FACEBOOK = "FACEBOOK",
  INSTAGRAMM = "INSTAGRAMM",
}

const social: SocialMedia = SocialMedia.FACEBOOK;
