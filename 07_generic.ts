const numberArray: Array<number> = [1, 1, 2, 3, 5, 8, 13];
const stringArray: Array<string> = ["pete", "molly"];

const reverse = <T>(array: T[]): T[] => array.reverse(); //to ask Andry more about

reverse(numberArray);
reverse(stringArray);
